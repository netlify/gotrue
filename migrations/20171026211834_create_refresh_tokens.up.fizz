create_table("refresh_tokens", func(t) {
	t.Column("id", "integer", {"primary": true})
	t.Column("instance_id", "string", {})
	t.Column("token", "string", {})
	t.Column("user_id", "string", {})
	t.Column("revoked", "boolean", {})
})

add_index("refresh_tokens", "instance_id", {})
add_index("refresh_tokens", ["instance_id", "user_id"], {})
add_index("refresh_tokens", "token", {})
